apiVersion: v1
kind: Service
metadata:
  name: metrovision-api
  labels:
    app: metrovision-api
spec:
  type: ClusterIP
  selector:
    app: metrovision-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
