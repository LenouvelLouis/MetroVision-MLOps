apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - route.yaml

# OpenShift: image from internal registry
images:
  - name: metrovision-api
    newName: image-registry.openshift-image-registry.svc:5000/metrovision/metrovision-api
    newTag: latest
